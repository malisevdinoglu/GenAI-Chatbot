# LangChain v0.1.16'ya sabitleyerek import hatalarını çözüyoruz.
# Bu versiyon, tüm modüllerin (ConversationalRetrievalChain vb.) doğru yerde olmasını sağlar.
langchain==0.1.16

# Diğer ana bağımlılıklar
pandas
google-cloud-aiplatform
langchain-google-vertexai
langchain-google-genai 

# --- KRİTİK DEĞİŞİKLİK: FAISS Kurulumu için Sistem Bağımlılıkları ---
# Streamlit Cloud'da faiss-cpu'dan önce build-essential paketini kurar.
!:apt install -y build-essential 

# Vektör veritabanı
faiss-cpu 

# Web arayüzü
streamlit